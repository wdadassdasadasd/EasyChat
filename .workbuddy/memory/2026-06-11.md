# 2026-06-11 工作日志

## EasyChat 全面工程审计

- 完成只读工程审计，生成报告 `EasyChat-全面工程审计与优化建议-2026-06-11.md`
- 总体健康评分: 3.4/5.0
- 发现 4 个 P0 + 8 个 P1 + 7 个 P2 + 5 个 P3 问题
- 最重要的 P0 问题: sandbox:false 导致 RCE 风险
- 与现有审计报告（聊天链路架构稳定性评估-2026-06-11.md）对比，确认了部分 P0 已修复（pong超时、队列看门狗、WAL checkpoint、pending恢复），发现新问题（安全漏洞、DB错误处理、replace失败补偿）
- 报告包含架构图、聊天链路图、Bug清单、专项评估、对标分析、修复Backlog和四阶段路线图

## P0 问题修复

- **P0-1**: 启用 sandbox:true，重构 preload 为白名单 API 模式
  - preload/index.js 不再暴露完整 ipcRenderer，改为暴露 electronAPI (白名单 channel) 和 api (命名方法)
  - 所有 renderer 文件从 `window.electron.ipcRenderer.xxx` 迁移到 `window.api.xxx`
  - 涉及文件: Login.vue, WinOp.vue, UserInfo.vue, UserInfoPassword.vue, Chat.vue, FileManage.vue, ChatMessageSearchDialog.vue, Request.js, useChatMessageSender.js, useChatMessages.js, useChatSessions.js, useFileTransfer.js, useMessageComposer.js
- **P0-2**: 移除 contextIsolation 回退路径，改为 console.error 拒绝暴露
- **P0-3**: DB 读错误显式传播到 renderer
  - IPC handler 层 (onLoadSessionData, onLoadChatMessage, onSearchChatMessage) 增加 try/catch 和 buildIpcErrorPayload
  - Renderer 层中文错误提示
- **P0-4**: HTTP 成功但 replace 失败的补偿机制
  - markMessageLocalSyncFailed 增加 scheduleLocalSyncRetry 后台重试（指数退避，最多3次）
  - localSyncRetryTimers 数组跟踪定时器，cleanupUploadControllers 中清理
  - ChatMessageModel 新增 findOrphanedPendingMessages 方法
  - ipc.js 新增 onRecoverLocalSyncFailed handler
- 测试更新: 3个测试文件从 mock window.electron.ipcRenderer 改为 mock window.api
- 全部 210 个测试通过

## 审计报告更新

- 根据 P0 修复结果更新审计报告 `EasyChat-全面工程审计与优化建议-2026-06-11.md`
- 总体评分从 3.4/5.0 提升到 3.8/5.0
- 安全与隐私评分从 2.5/5 提升到 3.5/5（+1.0，最大提升）
- P0 Bug 清单标记为"已修复 ✅"，添加详细修复说明
- 架构图、IPC 专项、安全评估、测试评估等章节同步更新
- 第一阶段路线图标记为已完成
- 添加附录 A: P0 修复变更日志（修改文件清单 + API 映射表）
- 报告从 973 行扩展到 1100 行
